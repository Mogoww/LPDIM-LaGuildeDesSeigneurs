<form method="post" action="{{ path('player_api_html_delete', {'identifier': player.identifier}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player.identifier) }}">
    <button class="btn">Delete</button>
</form>